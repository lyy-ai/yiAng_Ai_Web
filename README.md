# 技术培训网站

一个基于Flask的可扩展技术培训网站，支持动态添加栏目和内容。

## 功能特点

- ✅ **响应式设计**：完美支持PC、平板、手机端
- ✅ **课程管理**：展示课程列表、详情，支持分类筛选
- ✅ **视频教程**：视频资源管理和展示
- ✅ **技术文章**：文章发布和分类
- ✅ **团队介绍**：讲师团队展示
- ✅ **可扩展栏目**：通过配置文件轻松添加新栏目

## 技术栈

- **后端**：Flask 3.0
- **前端**：Bootstrap 5 + JavaScript
- **数据存储**：JSON配置文件

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 运行网站

```bash
python app.py
```

访问 http://localhost:5000

## 项目结构

```
yyy/
├── app.py                  # Flask主程序
├── config.json            # 网站配置文件
├── requirements.txt       # Python依赖
├── templates/             # HTML模板
│   ├── layout.html       # 基础布局
│   ├── index.html        # 首页
│   ├── courses.html      # 课程列表
│   ├── course_detail.html # 课程详情
│   ├── videos.html       # 视频教程
│   ├── articles.html     # 文章列表
│   ├── article_detail.html # 文章详情
│   ├── about.html        # 关于我们
│   └── custom_page.html  # 自定义页面模板
└── static/               # 静态资源
    ├── css/
    │   └── style.css     # 样式文件
    ├── js/
    │   └── main.js       # JavaScript文件
    └── images/           # 图片资源
```

## 如何扩展新栏目

### 方法1：使用自定义页面

在 `config.json` 中添加自定义页面配置：

```json
{
  "custom_pages": {
    "new_page": {
      "title": "新栏目标题",
      "icon": "🎯",
      "content_type": "html",
      "content": "<h2>这是新栏目的内容</h2><p>支持HTML格式</p>"
    }
  },
  "navigation": [
    // ... 其他导航项
    {
      "id": "new_page",
      "name": "新栏目",
      "url": "/custom/new_page",
      "icon": "🎯"
    }
  ]
}
```

访问：http://localhost:5000/custom/new_page

### 方法2：添加新的路由和模板

1. 在 `app.py` 中添加新路由：

```python
@app.route('/my_new_page')
def my_new_page():
    return render_template('my_new_page.html', config=config)
```

2. 在 `templates/` 中创建 `my_new_page.html`

3. 在 `config.json` 的 navigation 中添加导航链接

### 方法3：扩展现有模块

在 `config.json` 中添加新的数据类型，例如添加"项目案例"：

```json
{
  "projects": [
    {
      "id": 1,
      "title": "数据分析项目",
      "description": "项目描述",
      "image": "/static/images/project1.jpg"
    }
  ]
}
```

然后在 `app.py` 中添加相应路由，在 `templates/` 中创建模板。

## 自定义配置说明

### 修改网站信息

编辑 `config.json` 中的基本信息：

```json
{
  "site_name": "你的网站名称",
  "site_description": "你的网站描述"
}
```

### 添加课程

在 `config.json` 的 `courses` 数组中添加：

```json
{
  "id": 4,
  "title": "新课程标题",
  "description": "课程描述",
  "category": "分类名称",
  "level": "初级/中级/高级",
  "duration": "XX课时",
  "price": "¥XXX",
  "instructor": "讲师名称",
  "tags": ["标签1", "标签2"],
  "image": "/static/images/course.jpg"
}
```

### 添加文章

在 `config.json` 的 `articles` 数组中添加：

```json
{
  "id": 3,
  "title": "文章标题",
  "summary": "文章摘要",
  "content": "文章正文内容...",
  "author": "作者名称",
  "date": "2024-01-20",
  "category": "分类",
  "views": 0
}
```

## 样式自定义

修改 `static/css/style.css` 中的CSS变量：

```css
:root {
    --primary-color: #0d6efd;  /* 主色调 */
    --secondary-color: #6c757d; /* 次要色 */
}
```

## API接口

### 重新加载配置

```bash
POST /api/reload_config
```

用于动态更新配置而不重启服务器。

## 部署建议

### 开发环境
```bash
python app.py
```

### 生产环境

使用Gunicorn或uWSGI：

```bash
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

或使用Docker部署（需要创建Dockerfile）。

## 常见问题

**Q: 如何更改网站端口？**
A: 修改 `app.py` 最后一行的 `port=5000` 为你需要的端口号。

**Q: 如何添加数据库支持？**
A: 可以集成SQLAlchemy，将JSON配置迁移到数据库中，便于管理大量数据。

**Q: 支持用户登录吗？**
A: 当前版本为展示型网站，可以集成Flask-Login实现用户系统。

## 未来扩展方向

- [ ] 用户注册登录系统
- [ ] 课程购买和支付功能
- [ ] 学习进度追踪
- [ ] 评论和互动功能
- [ ] 后台管理系统
- [ ] 数据库支持（替代JSON文件）
- [ ] 搜索功能优化
- [ ] Markdown文章支持

## 许可证

MIT License

## 联系方式

如有问题或建议，欢迎联系。
